#include <stdio.h>
#include "main.h"

/**
 *add - adds two numbers
 *@x: must be of type int
 *@y: must be of type int
 *Return: the sum
*/

int add(int x, int y)
{
	int sum = x + y;

	return (sum);
}
