#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen - returns the length of a string
 *
 *@s: the string
 *
 *Return: The length of the string
*/

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
