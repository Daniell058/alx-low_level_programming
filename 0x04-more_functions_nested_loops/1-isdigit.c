#include "main.h"
#include <ctype.h>

/**
 *_isdigit - checks if a number is a digit
 *
 *@c:parameter
 *
 *Return: Either 0 or 1
*/

int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0)
}

